[build]
builder = "nixpacks"

[deploy]
startCommand = "python download_model.py && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn pec.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"